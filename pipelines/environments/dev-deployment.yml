# Dev environment deployment pipeline
# You can customize this file for dev-specific variables or steps

name: 'Dev-Deployment-$(Date:yyyyMMdd)$(Rev:.r)'

trigger:
  branches:
    include:
    - main
    - develop

pool:
  name: 'Container-Pool'

stages:
- template: ../azure-infrastructure-pipeline.yml
  parameters:
    environment: dev
